@model MobileConference.Models.PlatformModel

@*IF YOU WOULD EXTENDED THE PAGE TO SHOW DESCRIPTION ABOUT PLATFORM, YOU SHOULD UNCOMMENTED CODE BELLOW *@
@*@if (Model != null)
{
    <div  class="container_12">
        <div id="platform">
            <div>@Model.Title</div>
            <div>@Model.Description</div>
        </div>
    </div>
}*@

<div class="paginationWrapper">
    @Html.Action("AllMaterials","Widget", new{materialPlatformId = (Model==null?null:(int?)Model.Id), search = ViewBag.search})
</div>
